export interface IFilter {
    launchYear: number;
    launchSuccess: boolean;
    landSuccess: boolean;
  }


  export interface IProgram {
    image: string;
    title: string;    
    launchYear: string;
    launchSuccess: boolean;
}
